{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:asgi --reload --host 0.0.0.0 --port 8000",
    "migrate": "uv run alembic upgrade head",
    "revision": "uv run alembic revision --autogenerate -m",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  }
}
